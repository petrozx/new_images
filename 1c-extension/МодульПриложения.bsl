// Расширение / модуль управляемого приложения.
// Цель: задать заголовок окна без лишнего текста.
//
// Важно: суффикс «(1С:Предприятие КОРП)» добавляет платформа КОРП-лицензии.
// Штатными средствами убрать только слово «КОРП» нельзя — платформа
// всегда дописывает обозначение редакции. Метод УстановитьЗаголовок()
// меняет пользовательскую часть заголовка (слева), системный суффикс остаётся.
//
// Для скриншотов/документации используйте scripts/remove_korp_from_title.py
// или готовые файлы в images/.

#Область ПрограммныйИнтерфейс

&После("ПриНачалеРаботыСистемы")
Процедура УХ_БезКОРП_ПриНачалеРаботыСистемы()

	// Пользовательская часть заголовка (без суффикса платформы).
	КлиентскоеПриложение.УстановитьЗаголовок("Управление холдингом, редакция 3.3");

КонецПроцедуры

#КонецОбласти
